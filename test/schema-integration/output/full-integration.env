# Environment variables generated by Konfigo

DATABASE_CONNECTION_STRING=postgresql://${database.host}:${database.port}/${database.name}
DATABASE_POOL_SIZE=20
SERVICE_ENVIRONMENT=production
SERVICE_NAME=BASE-SERVICE
SERVICE_URL=${service.protocol}://${service.name}:${service.port}